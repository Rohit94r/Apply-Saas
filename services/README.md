# Services — backend microservices (optional Phase 2+)

| Service | Purpose |
|---------|---------|
| `realtime/` | WebSocket server for desktop live transcription streaming |

**Not needed until** desktop app requires lower latency than REST polling.

Web app continues using Next.js API routes for all features today.
